#include <stdio.h>
#include <string.h>

int main() {
	char s1[50];
	char s2[50];
	gets(s1);

	strcpy(s2, s1);
	strrev(s2);

	if(strcmp(s1, s2) == 0)
		printf("palindrome\n",s1);
	else
		printf("not a palindrome\n", s1);
	return 0;
}
